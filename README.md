# rainfall

## What this is

RainFall is a security project, organised like a CTF with levels.

The password to user `level0` is `level0`

## How to use

Just run `make start` ;).
You will then be able to connect to the virtual machine through ssh.
